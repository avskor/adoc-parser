[package]
name = "adoc-parser"
version = "0.1.0"
edition = "2024"
description = "Zero-dependency pull parser for AsciiDoc, producing an iterator-based event stream."
license.workspace = true
repository.workspace = true

[dependencies]
