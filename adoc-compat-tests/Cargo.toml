[package]
name = "adoc-compat-tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
adoc-parser = { path = "../adoc-parser" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
